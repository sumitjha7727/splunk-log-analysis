# Copy to .env (not committed) and set a real password before running
# `docker compose up`. Also used as splunk_uf_admin_password when
# running the Ansible playbooks (pass with -e or set in a vaulted
# group_vars file - don't commit it either way).
SPLUNK_PASSWORD=changeme-before-first-run
