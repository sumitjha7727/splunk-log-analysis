title: DHCP Lease Pool Dominated by a Single Host
id: 3554d043-aa9d-41e9-97fc-ac4639beedfb
status: experimental
description: >
  Flags a single MAC address accounting for an outsized share of DHCP
  lease activity - the signature of a boot-looping or misconfigured
  device repeatedly re-confirming the same lease, rather than a mapped
  attacker technique. Deliberately left without an ATT&CK tag: forcing
  a technique ID onto an operational anomaly would misrepresent what
  this actually detects. Threshold note (recalibrated 2026-09-18):
  Project 6 documented a severe Splunk line-breaking bug that
  undercounted DHCP events, and this rule's original threshold (>20)
  was calibrated against that undercounted view. With the ingestion
  fix in place and the true counts visible (top host 744 of 1,502
  records = 49.5%; next-highest 94 = 6.3%), >20 flags 9 of 87 MACs -
  too loose for a rule about one host dominating the pool - while >100
  isolates exactly the one dominant host, so the threshold is now 100.
  Expressed as an `event_count` correlation over dhcp-lease-event.yml's
  base rule rather than an inline aggregated condition - the original
  `condition: selection | count() by mac > 20` syntax this rule used is
  a deprecated Sigma v1 pipe-aggregation form that pySigma no longer
  parses (confirmed directly: `sigma check` rejected it with "The pipe
  syntax in Sigma conditions has been deprecated and replaced by Sigma
  correlations"). This UUID is unchanged from that version, so every
  other reference to this rule (the Project 7 detections table,
  savedsearches.conf, RUNBOOK.md) still points at the same detection.
references:
  - https://github.com/sumitjha7727/splunk-log-analysis/tree/main/06-dhcp-log-analysis
author: Sumit Kant Jha
date: 2026-08-30
modified: 2026-09-18
correlation:
  type: event_count
  rules:
    - 7641470d-17e4-4617-a8ac-e5daf1c61c35
  group-by:
    - mac
  timespan: 24h
  condition:
    gt: 100
falsepositives:
  - A DHCP relay or gateway device that legitimately renews leases on behalf of many downstream clients
  - A single busy access point or NAT device generating high normal lease-renewal volume
level: informational
