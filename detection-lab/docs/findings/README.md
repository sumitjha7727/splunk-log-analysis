# Findings

Empty for now. Once real Atomic Red Team campaigns are run against
this lab (see `../../attacks/run_campaign.py` and the build order in
`../../README.md`), each campaign's writeup goes here — one file per
campaign or per notable finding, following the same "what actually
happened, including what didn't work" convention as Projects 1-7's own
findings sections throughout this repo.

Nothing gets written here from assumption or simulation — only from an
actual campaign manifest and its correlated Splunk results, cross-checked
against `../coverage.md`.
