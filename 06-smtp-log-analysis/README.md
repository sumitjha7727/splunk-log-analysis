- **Two distinct scanning patterns, two different tools** — Query 5 (51 events: 30 Nmap + 21 Nessus) shows Nmap's SMTP script (`nmap.scanme.org` HELO) fired **30 times from 7 different sources** (`192.168.202.79`, `.100`, `.108`, `.136`, `192.168.203.45`, `.203.61`, `192.168.204.45`), spread across a wide range of destinations — a broad network-wide sweep pattern. Separately, Nessus's fingerprint HELO strings (`mail.nessus.org` and bare `nessus`) accounted for **21 events, almost entirely from `192.168.202.110`** (one additional event came from `192.168.202.138`), hitting 11 distinct destinations — including two separate hits against `192.168.22.102` under *each* HELO variant — a genuinely concentrated, repeated-target pattern versus Nmap's broad one-shot spread.
- **Correction (2026-08-28):** this finding originally stated Nmap fired from only 3 sources, each hitting one destination once — undercounting the real activity by roughly 10x. Root cause: Query 5's Statistics results are paginated, and the original review likely only checked the first page. Re-verified directly against Splunk; the numbers above are the corrected, complete picture. Documenting this rather than quietly overwriting it, same principle as every other data-quality note in this series.